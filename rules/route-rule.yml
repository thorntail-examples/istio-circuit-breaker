apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: wfswarm-istio-circuit-breaker-name-default
spec:
  destination:
    name: wfswarm-istio-circuit-breaker-name
  precedence: 1
  route:
  - labels:
      app: wfswarm-istio-circuit-breaker-name